/**
 * Config de la paginación por defecto. Completamente customizable para que se muestre la paginación en funcion
 * de este objeto
 */
export const PaginationFilters = {
  page: 1,
  adsPerPage: 1
};
